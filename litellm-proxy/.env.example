# Copy to .env and fill in. Never commit .env.

# Required: clients (PipelineLab backend) send this as Bearer / api_key
LITELLM_MASTER_KEY=sk-change-me-to-a-long-random-string

# At least one provider key is required for the models you use.
OPENAI_API_KEY=
COHERE_API_KEY=
